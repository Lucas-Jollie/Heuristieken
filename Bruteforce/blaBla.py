class Node:
    def __init__(self, cargo = None, prev = None, children = None):
        self.cargo = cargo
        self.prev = parent
        self.children[25]
    def __str__(self):
        return str(self.cargo)
        
node = Node('''''')
for i in range(len(genome)):
    node = node.children[genome[i + 1]]
        
